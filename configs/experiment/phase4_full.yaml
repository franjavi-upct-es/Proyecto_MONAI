# Fase 4: entrenamiento completo en el fold 0. Criterio de aceptación: DSC >= 0.45.
name: phase4_full
max_iterations: 50000
val_every: 500
patience: 20            # número de validaciones sin mejora antes de detenerse
grad_accum_steps: 1
log_every: 50
